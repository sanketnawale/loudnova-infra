# FAKE placeholder values only. Real credentials are injected at runtime and are never
# commitable. This Secret is consumed by the api/worker Pods via envFrom.
# base64("localdev-only") = bG9jYWxkZXYtb25seQ==
apiVersion: v1
kind: Secret
metadata:
  name: paymentops-secrets
  namespace: paymentops
type: Opaque
stringData:
  DATABASE_PASSWORD: "localdev-only"
  REDIS_PASSWORD: ""
