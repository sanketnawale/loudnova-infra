apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - deployment.yaml

configMapGenerator:
  - name: failover-controller-code
    namespace: dr-controller
    files:
      - controller.py

generatorOptions:
  disableNameSuffixHash: false
