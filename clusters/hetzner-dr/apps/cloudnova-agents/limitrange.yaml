apiVersion: v1
kind: LimitRange
metadata:
  name: cloudnova-agents-limits
  namespace: cloudnova-agents
spec:
  limits:
    - type: Container
      default:
        cpu: "250m"
        memory: "256Mi"
      defaultRequest:
        cpu: "50m"
        memory: "128Mi"