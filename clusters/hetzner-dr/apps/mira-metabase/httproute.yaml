apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mira-metabase
  namespace: mira-metabase
spec:
  parentRefs:
    - name: public-gateway
      namespace: gateway-infra
  hostnames:
    - stats.mira.cloudnova.tech
  rules:
    - backendRefs:
        - name: mira-metabase
          port: 80