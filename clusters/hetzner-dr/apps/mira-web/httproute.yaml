apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mira-web
  namespace: mira-web
spec:
  parentRefs:
    - name: public-gateway
      namespace: gateway-infra
  hostnames:
    - mira.cloudnova.tech
  rules:
    - backendRefs:
        - name: mira-web
          port: 80